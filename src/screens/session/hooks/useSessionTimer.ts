// Re-exported from the use_cases layer.
// Note: return fields renamed — elapsedSeconds (was elapsed), estimatedSampleCount (was sampleCount).
export { useSessionTimer } from "@/use_cases/useSessionTimer";
